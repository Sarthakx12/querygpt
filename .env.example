# Required for ASK mode (plain-English -> SQL). SQL mode works without it —
# everything in SQL mode runs locally with no network call.
#
# Get a key at https://platform.deepseek.com
DEEPSEEK_API_KEY=
